# 📋 قوانين تطوير المشروع - Development Rules

> **⚠️ تحذير هام:** هذا الملف يحتوي على القوانين الأساسية للتطوير.
> يجب قراءته بالكامل قبل إجراء أي تعديل على الكود.

---

## 🏗️ نظرة عامة على المشروع

### الغرض
تطبيق دردشة ذكي متخصص في Roblox Studio، يعمل بالكامل على الواجهة الأمامية مع حفظ محلي للبيانات.

### البنية التقنية
```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (React)                        │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────────────┐  │
│  │   Pages     │  │  Components  │  │        Hooks          │  │
│  │  - Index    │  │  - Chat/*    │  │  - useChat            │  │
│  │  - NotFound │  │  - UI/*      │  │  - useChatStore       │  │
│  └─────────────┘  └──────────────┘  └───────────────────────┘  │
│                              │                                   │
│                    ┌─────────▼─────────┐                        │
│                    │   Zustand Store   │                        │
│                    │   (chatStore.ts)  │                        │
│                    └─────────┬─────────┘                        │
│                              │                                   │
│                    ┌─────────▼─────────┐                        │
│                    │   localStorage    │                        │
│                    │  (persist layer)  │                        │
│                    └───────────────────┘                        │
└─────────────────────────────────────────────────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   Edge Function     │
                    │   (Supabase)        │
                    ├─────────────────────┤
                    │   Lovable AI        │
                    │   (Gemini 3.0)      │
                    └─────────────────────┘
```

---

## 📁 هيكل الملفات

```
src/
├── components/
│   ├── chat/           # مكونات الدردشة الرئيسية
│   │   ├── ChatContainer.tsx    # الحاوية الرئيسية
│   │   ├── ChatMessage.tsx      # عرض الرسائل
│   │   ├── ChatInput.tsx        # حقل الإدخال
│   │   ├── ChatHeader.tsx       # رأس الصفحة
│   │   ├── StreamingText.tsx    # عرض النص المتدفق
│   │   ├── ThinkingIndicator.tsx # مؤشر التفكير
│   │   ├── WelcomeScreen.tsx    # شاشة الترحيب
│   │   └── ConversationSidebar.tsx # الشريط الجانبي
│   └── ui/             # مكونات Shadcn/UI
├── hooks/
│   └── useChat.ts      # منطق إرسال الرسائل
├── stores/
│   └── chatStore.ts    # متجر الحالة المركزي
├── pages/
│   ├── Index.tsx       # الصفحة الرئيسية
│   └── NotFound.tsx    # صفحة 404
├── lib/
│   ├── utils.ts        # دوال مساعدة عامة
│   └── dateUtils.ts    # دوال تنسيق التاريخ (DRY)
├── test/               # ملفات الاختبار
└── index.css           # أنماط التصميم

supabase/
└── functions/
    └── chat/
        └── index.ts    # Edge Function للذكاء الاصطناعي
```

---

## 🚨 القواعد الحرجة - Critical Rules

### 1️⃣ لا تعديل بدون فهم الاعتماديات
```typescript
// ❌ ممنوع: التعديل المباشر بدون فهم التأثير
// ✅ مطلوب: تتبع جميع الملفات التي تعتمد على هذا الكود

// قبل التعديل، اسأل:
// - ما الملفات التي تستورد هذا الملف؟
// - ما الدوال التي تستدعي هذه الدالة؟
// - هل يوجد اختبارات تغطي هذا الكود؟
```

### 2️⃣ لا تكرار منطق — استخدم دوال مشتركة
```typescript
// ❌ ممنوع
function formatDateInComponent1(date: Date) { ... }
function formatDateInComponent2(date: Date) { ... }

// ✅ مطلوب: استخدام lib/dateUtils.ts
import { formatRelativeDate } from '@/lib/dateUtils';
// استخدم في أي مكان
```

### 3️⃣ كل ميزة جديدة يجب أن تكون قابلة للتوسعة
```typescript
// ❌ ممنوع: قيم ثابتة في الكود
const MAX_MESSAGES = 100;

// ✅ مطلوب: قيم قابلة للتخصيص
interface ChatConfig {
  maxMessages: number;
  // يمكن إضافة المزيد لاحقاً
}
```

### 4️⃣ كل وحدة جديدة يجب أن تكون مفصولة (Modular)
```typescript
// ❌ ممنوع: ملف ضخم يحتوي كل شيء
// ChatEverything.tsx (500+ سطر)

// ✅ مطلوب: ملفات صغيرة ومركزة
// ChatMessage.tsx (~100 سطر)
// ChatInput.tsx (~200 سطر)
// ChatHeader.tsx (~50 سطر)
```

### 5️⃣ كل دالة يجب أن تكون صغيرة وواضحة
```typescript
// ❌ ممنوع: دالة تفعل كل شيء
function processMessage(msg) {
  // 50+ سطر من المنطق المعقد
}

// ✅ مطلوب: دوال صغيرة ذات غرض واحد
function validateMessage(msg) { ... }
function formatMessage(msg) { ... }
function sendMessage(msg) { ... }
```

### 6️⃣ ممنوع المنطق المتداخل العميق بدون شرح
```typescript
// ❌ ممنوع: تداخل بدون تفسير
if (a) {
  if (b) {
    if (c) {
      // ماذا يحدث هنا؟!
    }
  }
}

// ✅ مطلوب: Early returns + تعليقات
// التحقق من صحة المدخلات
if (!a) return;

// التحقق من حالة المستخدم
if (!b) return;

// تنفيذ المنطق الرئيسي
if (c) {
  // شرح واضح للمنطق
}
```

---

## ⚠️ تحذيرات الكود الحساس

### 🔴 كود حرج - لا تعدل بدون مراجعة شاملة

```typescript
// ⚠️ WARNING: هذا الكود يتحكم في حفظ البيانات
// أي تعديل قد يتسبب في فقدان بيانات المستخدمين
// الملف: src/stores/chatStore.ts
// الدالة: persist middleware

// ⚠️ WARNING: هذا الكود يتحكم في الاتصال بالخادم
// أي تعديل قد يكسر الـ streaming
// الملف: src/hooks/useChat.ts
// الدالة: sendMessage

// ⚠️ WARNING: هذا الكود يتحكم في الأمان
// أي تعديل قد يفتح ثغرات أمنية
// الملف: supabase/functions/chat/index.ts
// الكود: Input validation
```

---

## 🔧 أنماط التصميم المستخدمة

### 1. State Management Pattern (Zustand)
```typescript
// المتجر المركزي يتبع نمط موحد:
// - State: البيانات
// - Actions: الدوال التي تعدل البيانات
// - Selectors: الدوال التي تقرأ البيانات
```

### 2. Component Composition Pattern
```typescript
// المكونات تتبع نمط التركيب:
// - Container: يدير الحالة والمنطق
// - Presentational: يعرض البيانات فقط
```

### 3. Custom Hook Pattern
```typescript
// الـ Hooks تتبع نمط:
// - تجريد المنطق المعقد
// - إعادة استخدام عبر المكونات
// - فصل الاهتمامات
```

---

## 🎨 قوانين التصميم

### Design Tokens فقط
```typescript
// ❌ ممنوع: ألوان مباشرة
className="text-white bg-black"

// ✅ مطلوب: Design Tokens
className="text-foreground bg-background"
```

### تناسق الأيقونات
```typescript
// ✅ جميع الأيقونات تستخدم:
// - strokeWidth={2}
// - أحجام موحدة (h-4 w-4، h-5 w-5)
```

### RTL Support
```typescript
// التطبيق يدعم العربية (RTL)
// - استخدم dir="auto" للنصوص
// - استخدم gap بدلاً من margin
```

---

## 📝 قوانين التوثيق

### توثيق الملف
```typescript
/**
 * @fileoverview وصف الملف ودوره في النظام
 * 
 * @description
 * شرح تفصيلي لما يفعله هذا الملف
 * 
 * @dependencies
 * - قائمة الاعتماديات الرئيسية
 * 
 * @impact
 * - ماذا يحدث لو تم تغيير هذا الملف
 */
```

### توثيق الدوال
```typescript
/**
 * وصف موجز للدالة
 * 
 * @description
 * شرح تفصيلي للمنطق
 * 
 * @param {Type} name - وصف المعامل
 * @returns {Type} وصف القيمة المرجعة
 * 
 * @throws {ErrorType} متى يحدث الخطأ
 * 
 * @example
 * // مثال على الاستخدام
 * const result = functionName(param);
 * 
 * @sideEffects
 * - التأثيرات الجانبية إن وجدت
 */
```

---

## 🔮 نقاط التوسعة المستقبلية

### Extension Points الحالية
```typescript
// 1. إضافة نماذج ذكاء اصطناعي جديدة
// الملف: supabase/functions/chat/index.ts
// النقطة: model parameter

// 2. إضافة أنواع مرفقات جديدة
// الملف: src/stores/chatStore.ts
// النقطة: Attachment.type

// 3. إضافة حالات رسائل جديدة
// الملف: src/stores/chatStore.ts
// النقطة: MessageStatus type

// 4. إضافة ثيمات جديدة
// الملف: src/index.css
// النقطة: CSS variables
```

### TODOs الاستراتيجية
```typescript
// TODO [FUTURE]: إضافة دعم المحادثات المشتركة
// TODO [FUTURE]: إضافة تصدير/استيراد المحادثات
// TODO [FUTURE]: إضافة البحث الدلالي في المحادثات
// TODO [SECURITY]: إضافة تشفير للبيانات المحلية
// TODO [PERFORMANCE]: إضافة Virtual Scrolling للمحادثات الطويلة
```

---

## ✅ قائمة التحقق قبل الـ Commit

- [ ] هل قرأت الكود المتأثر بالكامل؟
- [ ] هل فهمت الاعتماديات؟
- [ ] هل أضفت التوثيق المناسب؟
- [ ] هل استخدمت Design Tokens؟
- [ ] هل اختبرت على Mobile و Desktop؟
- [ ] هل تأكدت من عدم كسر أي وظيفة؟
- [ ] هل أضفت WARNING للكود الحساس؟
- [ ] هل الكود قابل للقراءة لمطور جديد؟

---

## 📚 مراجع مهمة

- [Zustand Documentation](https://docs.pmnd.rs/zustand)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Shadcn/UI](https://ui.shadcn.com)
- [React Best Practices](https://react.dev)

---

> **آخر تحديث:** 2025-02-04
> **المُعد:** Lovable AI Assistant
> **الإصدار:** 2.0 - تم إضافة dateUtils ودعم React Router v7 flags
